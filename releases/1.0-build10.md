# TAKE 47 · 1.0 (build 10) — 2026-07-04 · TestFlight — the build the first guests meet

## Release notes — for the Crew (and tonight, the Friends & Family)

Opening night. 🎬 Every icon in the game is now drawn by our own art department — the HOME button is a director's chair, prints are a proper film-print ticket, and the trophy shelf is officially THE AWARDS SHELF. New players get told what a print *is* the moment they earn one. Daily results now share like a film strip: 🟩🟨🟥. And if you finish a daily, the director offers — once, politely — to call you back at midnight when the new slate drops.

## QA report

**Method:** post-push browser gate on shipped code (full-engine parse, live-page runtime probe, visual pass on the drawn icon tier at real sizes on the deployed Pages build). Pages deploy: healthy again (GitHub-side outage cleared).

| Check | Verdict |
|-------|---------|
| Pipeline | ✅ 3m13s, uploaded to ASC |
| Parse + all Take-10 markers | ✅ PARSE-OK |
| Icon tier renders live (shelf/footrow/HOME/wallet/BUY) | ✅ screenshot-verified on deployed build |
| Notification toggle (web fallback label) | ✅ probed live |
| D-015 copy changes present | ✅ marker-verified |
| Haptics/notification/icons on-device | 🔄 pending Dan |
| DAILIES card notification offer flow on-device | 🔄 pending Dan |
| Dedicated copy read (deferred QA item) | ✅ ~200 strings + anomaly scan: zero typos, zero legacy leftovers in player copy (3 flags were dev comments/typeof guard); voice consistent. One editorial note logged (gauntlet register on the share card) |
| VoiceOver pass | 🔄 staged for Dan's device (checklist below) |

**VoiceOver checklist (Dan, ~10 min, Settings → Accessibility → VoiceOver):** home screen reads title → ACTION ticket → shelf buttons with lock hints → call sheet; every settings toggle announces its state; cast cards read name + bio (portraits are decorative/silent); a full take is playable by double-tap (canvas announces the control summary); DAILIES REPORT card buttons all reachable and labeled.

**Defects fixed in this build:** D-014 (icon system), D-015 (all copy verdicts).

## Known issues shipped

- QA Test Mode panel present (strip gate before submission; dev-facing beta strings on the same gate).
- Inset table is inference, not measurement (native plugin post-beta, BACKLOG).

## Store metadata snapshot

N/A — TestFlight. Build 9 is the Beta App Review vehicle ("Waiting for Review"); once approved, build 10 can join the external group without re-review (same version).
