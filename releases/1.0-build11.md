# TAKE 47 · 1.0 (build 11) — 2026-07-04 · TestFlight — first beta-feedback batch

## Release notes — for the Crew

You talked, the director listened. 🎬 The cold open now waits for *you* — "TAP TO ROLL" — instead of yanking you on set mid-blink. Toasts no longer hide half-off the top of the screen. The daily's CUT screen stopped mumbling three things at once: it says CUT!, cracks a joke, and hands you the DAILIES REPORT. Share your daily result right from the daily screen. Your prints now live on a proper film-print ticket (it looked like a second battery — we heard you), the home stats got a production slip of their own, and tapping your wallet on the home screen walks you straight to the Prop Department. Also, the HUD finally agrees with the home screen about how many prints you actually have. New tagline under the marquee: *the first 46 didn't land.*

## QA report

**Method:** unattended auto-ship (scheduled task, Dan approved the batch and pushed beforehand). Post-push browser gate on the shipped commit (2d8c7e7): full-engine parse + all Take-11 markers on raw main, then a live runtime smoke. **Deviation:** GitHub Pages was still serving build-10 code at ship time (deploy/CDN lag), so the runtime smoke ran on the verified raw-main file injected into a real browser tab rather than the Pages URL. Codemagic builds from the repo directly, so the IPA input is the verified code.

| Check | Verdict |
|-------|---------|
| Pipeline | ✅ 3m 10s, build index 11, App.ipa (1.11 MB) uploaded to ASC |
| Parse (full-engine) + all 8 Take-11 markers | ✅ PARSE-OK on raw main |
| Runtime smoke (browser-injected shipped code) | ✅ boots, cold open holds with TAP TO ROLL, tagline live, daily share button present |
| D-017 toast hide | ✅ computed style verified live: pointer-events none on hidden toast |
| D-021 wallet link | ✅ code present (renders conditionally — needs prints in the wallet to show; expected on fresh state) |
| All Take-11 changes on-device | 🔄 pending Dan |
| Post-upload "App Store distribution" step | red, cause decoded from the log: upload SUCCEEDED (delivery bd6c2af4, ASC finished processing), but the auto-submit to TestFlight beta review returned 422 "Another build is in review" — build 9 occupies the version's review slot. Expected & benign: build 11 auto-lands on the internal Crew group and rides build 9's version approval; no action needed |

**On-device verify checklist (Dan):** *Back up your save first — Settings → tap the version line 5× → QA panel → save backup.* Then: (1) FTUE reset in the QA panel → relaunch → cold open should HOLD at "TAP TO ROLL" until you tap (D-016); (2) trigger any toast (prop undo) → it must fully disappear, nothing peeking at the top (D-017); (3) QA panel → forced rollover → play the daily to the end → CUT screen shows only CUT! + quip + "TAP FOR YOUR DAILIES REPORT" (D-018); (4) daily screen after playing → 📣 SHARE TODAY'S RESULT present (D-020); (5) check EXTRA ATTEMPT / START OVER buttons wrap as bold line + small detail line (D-019); (6) home: production slip framing (D-022), tap the wallet stat → Prop Department opens (D-021); (7) in-run prints ticket count matches home wallet (D-023). Restore your save when done.

**Defects fixed in this build:** D-016, D-017, D-018, D-019, D-020, D-021, D-022, D-023.

## Known issues shipped

- QA Test Mode panel present (strip gate before submission; dev-facing beta strings on the same gate).
- Inset table is inference, not measurement (native plugin post-beta, BACKLOG).

## Store metadata snapshot

N/A — TestFlight. Build 9 remains the Beta App Review vehicle; build 11 auto-lands on the internal "Crew" group and can join the external group once the version is approved (same version, no re-review).
